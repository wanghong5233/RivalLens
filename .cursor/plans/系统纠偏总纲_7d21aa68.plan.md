---
name: 系统纠偏总纲
overview: RivalLens 从 demo 到成熟的系统纠偏一级 plan(活文档)。统一命名为阶段主轴 S0-S5,每个阶段=一个 todo=指定一个二级 plan 去 build,二级验证无误后回写本总纲,二级执行中发现的新结构性问题迭代纠正本总纲。
todos:
  - id: S0
    content: S0 正确性止损 [已完成]:S0-1 契约归一化 / S0-2 batch 截断 / S0-3 QA→researcher cap / S0-4 harness repair 回灌。已落地,docker 221/222,证据见 §4.1。提交 60714f3/de375ab。
    status: completed
  - id: S1
    content: S1 默认维度与上限单一事实源 [已完成]:S1-1 默认焦点维度收敛到 core/defaults.py / S1-2 竞品任务上限收敛 + discovery→research 丢弃可观测。提交 4f60ee8/d038cca,docker 全量 223 passed。证据见 §4.3。
    status: completed
  - id: S2
    content: S2 内容可观测合同 [已完成]:S2-1 LLM trace 持久化 / S2-2 run summary / S2-3 discovery 审计 / S2-4 QA finding / S2-5 researcher bind_step / S2-6 交错时间线 / S2-7 progress 降噪 / S2-8 llm.call start 降噪 / S2-9 provider/client error 去重。提交 49a2785/f627a72/953f613/50b5d4b。证据见 §4.4。
    status: completed
  - id: S3
    content: S3 数据质量 [已完成]:S3-1 discovery grounding/相关性/去重 / S3-2 去 researcher 合成证据 / S3-3 质量原则回写。提交 8d9c46d/d1665d6。证据见 §4.5。
    status: completed
  - id: S4
    content: S4 naive→成熟:S4-1 writer 章节对齐 / S4-2 contract 处置统一 / S4-3 intake 双轨 / S4-4 supervisor 维度启发式 / S4-5 魔法数进 config / S4-6 prompt 域偏置 / S4-7 skeleton 残留。进入时调研产出二级 plan。
    status: pending
  - id: S5
    content: S5 QA/agent 成熟度:S5-1 QA promoted enforcement 后台 count=0 + conftest mock 覆盖缺失致降级(原 D1)。补真实成熟实现,不为修绿 fake-mock smoke 改测试。进入时调研 qa/engine、skill_store 后台单例、conftest mock 产出二级 plan。
    status: pending
isProject: false
---

# RivalLens 系统纠偏总纲(活的一级 plan)

> 这是一级 plan,不是普通文档。闭环:每个阶段(todo)= 一个任务 → 指定二级 plan 去 build → 二级验证无误回写本总纲 → 二级执行中发现新结构性问题则迭代纠正本总纲。证据来自 run_4820c8cc8911 容器日志 + 对 [backend/app](backend/app) 的只读审计,引用带 file:line。

## 命名体系(统一为阶段主轴)

- 阶段(一级 todo):`S0`-`S5`,按依赖顺序推进,前一阶段 parity 稳定再开下一阶段。
- 问题条目:`SX-N` + 性质标签(`[正确性]`/`[可观测]`/`[数据质量]`/`[成熟度]`)。
- 二级刀:`SX-A`/`SX-B`(二级 plan 内的原子提交单元)。
- 旧编号映射:A1-A4→S0-*;C1/C2→S1-*;B1-B9→S2-*;C3→S3-1、A5→S3-2;C4-C10→S4-*;D1→S5-1。

## 0. 一句话结论

Harness 已接入主链路,但校验层曾是"硬拒绝"而非"归一化"(已在 S0 修复)。S1/S2/S3 已分别收敛单一事实源、内容可观测与数据质量底线。剩余问题集中在大量 naive/硬编码/双轨(S4)与 QA promoted enforcement 在后台 run 失效(S5)。

## 0.1 S3 后统一质量原则

ToB 竞品分析宁可留白也不能伪造。S3 后所有 discovery/research/report 链路继续遵循三条硬原则:

1. **质量优先于延迟/成本**:不为省 token/延迟把语义任务降级成脆弱硬规则。
2. **LLM-first,代码做护栏**:竞品识别、相关性、别名归一等语义判断优先交给模型;确定性代码负责 schema、去重、grounding 校验、可观测字段。
3. **No fabrication**:竞品与证据必须来自真实工具观测且可溯源;拿不到证据就标记未覆盖或整体降级,不得合成假竞品、假 quote、假 source。

## 1. 主链路实证(run_4820c8cc8911)

- supervisor harness 5 迭代失败 3 次(已由 S0 消除):iter2 `topics: at most 8 items, not 10`;iter3 `dimension must match ^[a-z]...`;iter5 `section_id must match ...`。每次失败=3×LLM 调用+降级。
- discovery 出 10 个离散竞品与查询跑题;discovery 上限 10、batch 上限 8 → 静默丢 2,无可观测字段。

## 2. 阶段路线与问题清单

### S0 正确性止损 [已完成]

把"拒绝式校验"改为"归一化优先",消除 supervisor 3/5 空转与 latent crash。

- S0-1 [正确性] 契约归一化:[backend/app/schemas/contracts.py](backend/app/schemas/contracts.py) `_validate_contract_token` slugify 替代 hard-reject;`validate_token_list` 跳过不可归一项。
- S0-2 [正确性] batch 截断:[backend/app/schemas/supervisor.py](backend/app/schemas/supervisor.py) `ConductResearchBatch.topics` 去 `max_length`,field_validator 截断至 8。
- S0-3 [正确性] latent crash:[backend/app/agents/nodes/supervisor.py](backend/app/agents/nodes/supervisor.py) `_decision_from_qa_feedback` QA→researcher 分支补 `competitors[:8]`。
- S0-4 [正确性] harness repair 回灌:[backend/app/service/llm/harness.py](backend/app/service/llm/harness.py) repair 把上一版非法输出拼到 prompt 前。
- 证据见 §4.1。

### S1 默认维度与上限单一事实源 [已完成]

二级 plan:`phase1_single_source`(刀 S1-A/S1-B)。

- S1-1 [数据一致性] 默认焦点维度 `("feature","pricing","user_feedback")` 散落 7 处:[supervisor.py](backend/app/agents/nodes/supervisor.py):99/103、[planner.py](backend/app/agents/nodes/planner.py):40、[researcher.py](backend/app/agents/nodes/researcher.py):35、[agent_outputs.py](backend/app/schemas/agent_outputs.py):10、[agent_outputs_pipeline.py](backend/app/schemas/agent_outputs_pipeline.py):158、[run_rt.py](backend/app/router/run_rt.py):569/577。收敛到 `core/defaults.py`。
- S1-2 [数据一致性] 竞品/任务上限不一致 + 静默丢弃:discovery `[:10]`(pipeline 69/277)、plan reconcile/batch `[:8]`(planner 148、supervisor 190/417、supervisor schema 50),planner 与 pipeline 还重复定义 MAX_*。统一 cap + discovery→research 收窄处补 `dropped_competitors` 日志。

### S2 内容可观测合同 [已完成]

二级 plan:`s2_observability_contract`(刀 S2-A/S2-B/S2-C/S2-D)。

- S2-1 [可观测] 全文 prompt/response 不可见:[llm_call.py](backend/app/models/llm_call.py) 只存 hash;[client.py](backend/app/service/llm/client.py):228-236 只打 preview_len。
- S2-2 [可观测] 无 run 级成本/时延汇总日志(仅 /metrics API 有聚合)。
- S2-3 [可观测] discovery 不可审计:[discovery.py](backend/app/agents/nodes/discovery.py):155-161 只打 count,无名单/snippet。
- S2-4 [可观测] QA 失败细节缺失:[qa/engine.py](backend/app/service/qa/engine.py) `qa.fast_path` 无 failed_rule_ids;semantic finding 只入 DB。
- S2-5 [可观测] researcher 子图无 bind_step/node.*,工具 args/observation 不落库。
- S2-6 [可观测] 无单条交错时间线(Step 与 SupervisorDecisionRecord 两表分离)。
- S2-7 [噪声] `run.progress` 每 60s 刷屏([run_rt.py](backend/app/router/run_rt.py):47)。
- S2-8 [噪声] `llm.call.start`+`finish` 成对 + repair 链一次业务调用 6+ 条。
- S2-9 [噪声] provider/client 双层 `llm.call.error` 字段不一致,告警去重难。

### S3 数据质量 [已完成]

二级 plan:`s3-data-quality`(刀 S3-A/S3-B/S3-C)。依赖 S2 可观测验证效果。

- S3-1 [数据质量] discovery grounding/相关性/去重(原 C3):`DiscoveryExtractOutput` 升级为候选结构 `{name,is_competitor,relevance_reason,evidence_quote}`;DISCOVERY_EXTRACT prompt 禁止臆造;discovery 后处理过滤非竞品/重复别名/grounding miss;fallback 返回空候选。
- S3-2 [数据质量] 去 researcher 合成证据(原 A5):`_fallback_action` 移除合成 `extract_structured` 分支;真实工具耗尽后 finalize;writer/analyst 对部分无证据维度打 `uncovered_*` risk flag,全空仍降级。
- S3-3 [数据质量] 质量原则回写:质量优先、LLM-first、No fabrication 写入 §0.1,作为 S4/S5 后续重构约束。

### S4 naive→成熟 [pending]

进入时调研相关代码产出二级 plan。

- S4-1 [成熟度] writer 章节-维度对齐靠别名表+轮询:[writer.py](backend/app/agents/nodes/writer.py):46-52/66-82。
- S4-2 [成熟度] contract 处置四种不一致(拒绝/静默改写/丢弃/跳过),需统一策略:researcher 136-138/191、writer/section。
- S4-3 [成熟度] intake 关键词表确定性解析与 harness 双轨:[intake.py](backend/app/agents/nodes/intake.py):45-71/276-359。
- S4-4 [成熟度] supervisor 维度推导启发式:[supervisor.py](backend/app/agents/nodes/supervisor.py):40-47/91-109 `DIMENSION_HINTS` 子串匹配。
- S4-5 [成熟度] 散乱魔法数进 config:researcher 34-38、planner、MAX_SUPERVISOR_ITERATIONS、各类切片。
- S4-6 [成熟度] prompt 域偏置:prompts.py 与 intake 258 硬编码 AI 编程示例。
- S4-7 [成熟度] skeleton 残留:supervisor 774/278、run_rt 106。

### S5 QA/agent 成熟度 [pending]

补真实成熟实现,不为修绿 fake-mock smoke 而改测试。进入时调研 qa/engine、skill_store 后台单例可见性与 scan 时序、conftest mock 覆盖,产出二级 plan。

- S5-1 [成熟度] QA promoted-rule enforcement 在后台 run task 里 count=0(后台 `skill_store.scan` 扫到 `skill_count=0`,测试写入 tmp 的 promoted YAML 未被单例加载)+ conftest fake LLM mock 缺 analyst/extract/qa-semantic 覆盖致整链降级。S2 收尾稳定复现两例(`enforced_yaml` + `then_writer_redo`)。详见 §4.2。对照 [docs/2.5-agent-architecture.md](docs/2.5-agent-architecture.md) §3.7:架构定义 QA=规则 DSL+LLM 语义+promoted enforcement,实现层在后台 run+降级态未达成。

## 3. Gap vs 赛题目标([docs/0-problem-background.md](docs/0-problem-background.md))

- 多 Agent 可信度(35%):QA 路由是规则表非推理(S4-4)、结构化 Schema 一致性曾被拒绝式校验反噬(S0,已修)。
- 技术深度/可观测(25%):全文 prompt/response、run 级成本、discovery/QA 细节缺日志(S2)。
- 溯源:researcher fallback 合成无 URL 证据(S3-2)。
- 业务价值:discovery 跑题+竞品静默丢弃影响覆盖度(S1-2/S3-1)。

## 4. 活文档增补(执行中发现,新→旧)

### 4.5 S3 收尾(已完成)

S3-A/S3-B 已落地。Discovery extraction 从纯名单升级为 grounded candidates;prompt 明确只能从 `search_results` 提取,`build_discovery_extract_fallback_user_prompt` 不再产出 `Name1/Name2/Name3` 占位假名;`discovery_node` 新增非竞品过滤、别名归一去重、`evidence_quote` snippet grounding 校验,并在 `discovery.complete`/Step payload 记录 `filtered_out_competitors` 与 `relevance`。Researcher fallback 移除 synthetic `extract_structured` 种子文本,真实搜索/抓取耗尽后直接 finalize;`WriterReportOutput` 对有证据 section 保留、对缺证据 target section 标记 `uncovered_section:*`,`AnalystOutput.build_fallback` 标记 `uncovered_dimension:*`,全空证据仍失败/降级,不做全局无 URL 一刀切。提交:`8d9c46d`/`d1665d6`。验证:S3-A targeted `16 passed`,phase3 events `8 passed`;S3-B focused `31 passed`;docker 全量最终 `pytest tests` = **235 passed / 2 failed**。2 例失败仍为 S5-1 promoted QA 后台 enforcement(`test_promoted_qa_rule_blocks_report_with_enforced_yaml` + `test_promoted_qa_rule_blocks_then_writer_redo_passes`),非 S3 回归。两刀 staged secret scan 均通过。

### 4.4 S2 收尾(已完成)

S2-A..S2-D 已落地。`llm_calls` 扩展 redacted/capped `prompt_text`、`response_content`、`response_raw`、`prompt_preview`、`fallback_used`、`fallback_reason`,普通日志仍不输出全文 prompt/raw response；`/api/runs/{run_id}/trace` 保持原 `steps`/`supervisor_decisions` 兼容,新增默认安全的 `llm_calls` 与 Step/Decision/LLMCall 交错 `timeline`；run 终态新增 `api.run.summary` 汇总 token、latency、coverage、QA rejection、supervisor iterations；Discovery/QA/Researcher 日志补齐 snippet sample、failed rule ids、semantic finding preview、tool latency/source_type_distribution,Researcher subgraph 与 channel harness 日志可绑定同一 `step_id`；`run.progress` 与 `llm.call.start` 降为 debug,provider 层错误改为 `llm.provider.error`,client 层保留唯一业务级 `llm.call.error`。提交:`49a2785`/`f627a72`/`953f613`/`50b5d4b`。验证:S2-B targeted `4 passed`;S2-C targeted `24 passed`;S2-D targeted `11 passed`;docker 全量最终 `pytest tests` = **228 passed / 2 failed**(S2 较 S1 净增 7 个 trace/redaction/discovery/metrics 用例全过);2 例失败为 S5-1 两例(`test_promoted_qa_rule_blocks_report_with_enforced_yaml` + `test_promoted_qa_rule_blocks_then_writer_redo_passes`),S2-C 对 [qa/engine.py](backend/app/service/qa/engine.py) 仅抽取 `_build_qa_fast_path_log_fields`/`_build_qa_slow_path_log_fields` 日志字段,未触 promoted 判定逻辑,非 S2 回归,详见 §4.2。每刀 staged secret scan 均通过。

### 4.3 S1 收尾(已完成)

S1-A/S1-B 已落地。新增 [backend/app/core/defaults.py](backend/app/core/defaults.py) 作为默认维度与实体上限单一事实源；`DEFAULT_FOCUS_DIMENSIONS` 替代生产代码默认三元组；`MAX_RESEARCH_COMPETITORS`/`MAX_DISCOVERY_COMPETITORS`/`MAX_TOTAL_PLAN_TASKS`/`MAX_WRITE_SECTIONS` 替代 planner、pipeline、supervisor、schema 的重复 cap 定义。`planner.reconcile_plan_tree_after_discovery` 在 discovery 结果超过 research cap 时新增 `planner.reconcile.discovery_capped` 结构化日志，包含 `cap`、`kept_count`、`discovered_count`、`dropped_competitors`。codex 另补 `test_plan_reconcile` 用例验证 `discovery_capped` 日志字段。提交:`4f60ee8`/`d038cca`。验证:docker 全量 `pytest tests` = **223 passed**（222 原有 + 1 新增 reconcile 用例，0 failed；S5-1 本次碰巧通过，见 §4.2 flaky 说明）。无 S1 回归。

### 4.1 S0 收尾(已完成)

S0-1..S0-4 已落地(见 §2 S0)。附带清理 harness 推广遗留测试债:test_researcher_subgraph/dispatcher 的 stale `get_llm_client` patch 改到 `service.llm.harness.get_llm_client`。提交:`60714f3`/`de375ab`。验证:docker 全量 `pytest tests` = 221 passed / 1 failed(唯一失败 = S5-1,正交)。本地 Windows 另有 9 个 async-postgres e2e 因 ProactorEventLoop 红,非代码 bug,docker 全绿。

### 4.2 S5-1 QA enforcement 后台失效(新发现,源自 S0 收尾全量验证)

现象:[test_smoke.py](backend/app/tests/test_smoke.py) `test_promoted_qa_rule_blocks_then_writer_redo_passes` 首轮 `qa_outcome` 实为 approved,期望 rejected;同类 enforce/visible 测试通过 → enforcement 主链路本身可用。根因(待 S5 二级 plan 精确定位):加载链路 `_load_promoted_qa_rules_from_skill_store()`([qa/engine.py](backend/app/service/qa/engine.py):180-196)在后台 task `count=0`;mock 覆盖 conftest `_FakeLLMClient` 无 analyst/extract/qa-semantic 分支致整链降级。

**认知更新(S1 收尾实测)**:该用例曾表现为 **flaky / race**——S1 期单独连跑 3 次为 失败/通过/失败,S1 全量跑碰巧通过(故 §4.3 显示 223 passed)。

**认知更新(S2 收尾实测,2026-06-05)**:S2 全量与单跑均**稳定复现**两例失败(`enforced_yaml` + `then_writer_redo`),不再 flaky。单跑日志直接给出确定性根因:后台 run task 内 `skill_store.scan.finish` 报 `skill_count=0`(skills_dir 指向测试 tmp 目录却扫到 0 条),进而 `qa.promoted_rules count=0`、`promoted_qa_enforced_count=0`,QA 不 block → `qa_outcome=approved`。即测试写入 tmp 的 promoted YAML 未被后台 task 的 skill_store 单例加载。范围比原先认知更大(波及 `enforced_yaml`,非仅 `then_writer_redo`),但根因从"疑似时序 race"收敛为"**skill_store 后台单例 skills_dir 可见性 / scan 时机**"这一确定性问题,利于 S5 二级 plan 直接定位。S5 修真实实现(稳定化后台 task 内 skill_store 加载),不改测试断言。

## 5. 明确不做(YAGNI)

- 不重写 LLMClient/provider 底层重试与 timeout;不引入新依赖(instructor/outlines);本轮不动 FE/SSE 协议;不把 QA 硬规则/forced_complete 塞进 harness 内部(保持 validation 与 business policy 分离)。

## 6. 方法论(双层文档 + PIV + 活文档闭环)

进入某阶段:新 session → 详细调研该段代码 → 产出该阶段二级 plan(刀/文件/改法/Verify,按"单一关注点·同风险·共享验证"分组)→ 逐刀 PIV,一刀一组原子提交;动手前先跑全绿快照作 parity 锚,同一刀连失败 3 次则 revert 重拆;阶段完成或发现新结构性问题 → 回写本总纲。详见 [docs/private/engineering-playbook/03-incremental-refactor-layered-piv.md](docs/private/engineering-playbook/03-incremental-refactor-layered-piv.md)。
