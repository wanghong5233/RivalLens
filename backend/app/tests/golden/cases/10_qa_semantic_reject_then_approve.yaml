id: 10_qa_semantic_reject_then_approve
description: Semantic QA rejects first then approves after writer rewrite.
setup:
  promoted_qa_rules: []
input:
  user_query: "semantic-reject-demo"
  competitors:
    - comp_cursor
  domain_hint: ai coding assistants
  reference_urls: []
  target_roles:
    - pm
assertions:
  final_qa_outcome: approved
  qa_rejection_count_gte: 1
