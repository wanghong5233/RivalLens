id: 13_deep_short_report_blocks
description: Deep reports that stay at demo-length must be rejected by deterministic QA gates.
input:
  user_query: "deep report short baseline gate"
  competitors:
    - comp_cursor
  domain_hint: ai coding assistants
  reference_urls: []
  target_roles:
    - pm
  report_depth: deep
assertions:
  final_qa_outcome: force_degraded
  qa_rejection_count_lte: 1
