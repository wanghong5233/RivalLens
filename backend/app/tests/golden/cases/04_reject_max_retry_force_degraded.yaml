id: 04_reject_max_retry_force_degraded
description: Data-degraded writer output should force finalize without repeated retry churn.
setup:
  promoted_qa_rules: []
  llm_profile: semantic_force_degraded
input:
  user_query: "data-degraded writer output should finalize without retry churn"
  competitors:
    - comp_cursor
  domain_hint: ai coding assistants
  reference_urls: []
  target_roles:
    - pm
assertions:
  final_qa_outcome: force_degraded
  qa_rejection_count_lte: 1
