id: 12_load_skill_progressive_disclosure
description: Researcher should invoke load_skill before structured extraction.
setup:
  promoted_qa_rules: []
  llm_profile: progressive_disclosure
input:
  user_query: "researcher should use progressive disclosure before extraction"
  competitors:
    - comp_cursor
    - comp_windsurf
  domain_hint: ai coding assistants
  reference_urls: []
  target_roles:
    - pm
assertions:
  final_qa_outcome: approved
  must_include_collector_action: load_skill
