id: 08_online_fail_fallback_offline
description: Online channel failure should fallback to offline snapshot and still approve.
setup:
  promoted_qa_rules: []
input:
  user_query: "online-fail fallback demo"
  competitors:
    - comp_cursor
  domain_hint: ai coding assistants
  reference_urls:
    - https://cursor.com/pricing
  target_roles:
    - pm
assertions:
  final_qa_outcome: approved
  qa_rejection_count_gte: 0
