interface:
  display_name: "PR Workflow"
  short_description: "Prepare scoped branches and PRs following the team's PR conventions."
  default_prompt: "Use the root PR workflow before preparing or creating a pull request."
